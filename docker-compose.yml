services:
  zerobitch-fleet:
    build: .
    env_file:
      - .env
    ports:
      - "4100:8080"
    volumes:
      - ./data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: unless-stopped
